{
  "name": "miniature",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "python3 -m http.server 3000",
    "build": "node -e \"require('fs').mkdirSync('dist',{recursive:true}); require('fs').copyFileSync('index.html','dist/index.html'); console.log('static build complete')\"",
    "start": "python3 -m http.server 3000",
    "lint": "node -e \"console.log('no lint step for static build')\"",
    "typecheck": "node -e \"console.log('no typecheck step for static build')\""
  },
  "engines": {
    "node": "20.x"
  }
}
